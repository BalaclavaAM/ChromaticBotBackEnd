[project]
name = "ChromaticBotBackEnd"
version = "0.1.0"
description = "FastAPI backend for sorting Spotify albums by chromatic analysis"
authors = [
    {name = "JuanMa", email = "68751794+BalaclavaAM@users.noreply.github.com"},
]
dependencies = [
    "fastapi>=0.121.0",
    "uvicorn[standard]>=0.38.0",
    "pydantic>=2.12.3",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
    "pymongo>=4.15.3",
    "pillow>=12.0.0",
    "colorthief>=0.2.1",
]
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false

[tool.pdm.scripts]
start = "uvicorn app.main:app --reload --host 0.0.0.0 --port 8080"
prod = "uvicorn app.main:app --host 0.0.0.0 --port 8080 --workers 2"
dev = "uvicorn app.main:app --reload"
